#pragma once

bool isLeapYear(unsigned year);

unsigned getDaysInMonth(
	unsigned month,
	unsigned year);

void incrementDate();

void incrementDate(char* date);

bool addDate();

char* getDate();

void displayDate();
